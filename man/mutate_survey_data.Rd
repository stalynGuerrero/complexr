% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mutate_survey_data.R
\name{mutate_survey_data}
\alias{mutate_survey_data}
\title{Mutate survey data using declarative definitions}
\usage{
mutate_survey_data(data, definitions, overwrite = TRUE)
}
\arguments{
\item{data}{A tibble with survey microdata.}

\item{definitions}{Named list of formulas defining new variables.}

\item{overwrite}{Logical. If FALSE, prevents overwriting existing variables.}
}
\value{
A tibble with new variables added or modified.
}
\description{
Adds or transforms variables in survey microdata using a named list
of formulas. Each definition must be a one-sided formula of the form
\code{~ expression}, evaluated within the data context.
}
\examples{
data <- tibble::tibble(
  ingreso = c(100, 200),
  miembros = c(2, 4)
)

mutate_survey_data(
  data,
  list(
    ingreso_pc = ~ ingreso / miembros
  )
)
}
